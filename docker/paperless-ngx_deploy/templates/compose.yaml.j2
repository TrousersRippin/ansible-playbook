---
services:
  {{ APP }}:
    image: {{ IMAGE_NAME }}:{{ IMAGE_TAG }}
    container_name: {{ APP }}
    hostname: {{ APP }}
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    env_file: .env
    volumes:
      - /opt/stacks/{{ APP }}/consume:/usr/src/{{ APP }}/consume:rw
      - /opt/stacks/{{ APP }}/data:/usr/src/{{ APP }}/data:rw
      - /opt/stacks/{{ APP }}/export:/usr/src/{{ APP }}/export:rw
      - /opt/stacks/{{ APP }}/media:/usr/src/{{ APP }}/media:rw
    ports:
      - {{ PORT_NUMBER }}:{{ PORT_NUMBER }}/tcp
    networks:
      - {{ NETWORK }}

networks:
  {{ NETWORK }}:
    external: true
