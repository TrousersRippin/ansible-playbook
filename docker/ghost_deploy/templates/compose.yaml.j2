---
services:
  {{ APP }}:
    image: {{ IMAGE_NAME }}:{{ IMAGE_TAG }}
    container_name: {{ APP }}
    hostname: {{ APP }}
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    environment:
      - TZ=Europe/Stockholm
      - PUID=1000
      - PGID=1000
    volumes:
      - /opt/stacks/{{ APP }}/config/config.production.json:/var/lib/ghost/config.production.json:ro
      - /opt/stacks/{{ APP }}/config:/var/lib/ghost/content:rw
    ports:
      - {{ PORT_NUMBER }}:{{ PORT_NUMBER }}/tcp
    networks:
      - {{ NETWORK }}

networks:
  {{ NETWORK }}:
    external: true
