---
services:
  {{ APP }}:
    image: {{ IMAGE_NAME }}:{{ IMAGE_TAG }}
    container_name: {{ APP }}
    hostname: {{ APP }}
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    environment:
      - TZ=Europe/Stockholm
    volumes:
      - /opt/stacks/{{ APP }}/cache:/cache:rw
      - /opt/stacks/{{ APP }}/config:/config:rw
      - /etc/localtime:/etc/localtime:ro
    ports:
      - {{ PORT_NUMBER }}:{{ PORT_NUMBER }}/tcp
    healthcheck:
      disable: true
    user: ${UID:-1000}:${GID:-1000}
    networks:
      - {{ NETWORK }}

networks:
  {{ NETWORK }}:
    external: true
