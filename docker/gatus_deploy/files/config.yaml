---
endpoints:
  - name: The Noisy Keyboard
    group: websites
    url: https://blog.thenoisykeyboard.com/
    interval: 1m
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 1000"

metrics: true

storage:
  type: memory

web:
  port: 8899
