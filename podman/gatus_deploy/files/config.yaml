---
web:
  port: 8899

metrics: true

storage:
  type: memory

# alerting:

endpoints:
  - name: The Noisy Keyboard
    group: websites
    url: https://blog.thenoisykeyboard.com/
    interval: 1m
    conditions:
      - "[STATUS] == 200"
      - "[RESPONSE_TIME] < 1000"
