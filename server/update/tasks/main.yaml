---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: "{{ apt_cache_valid_time }}"

- name: Upgrade all packages
  ansible.builtin.apt:
    upgrade: dist
  register: apt_upgrade_result

- name: Display upgraded packages
  ansible.builtin.debug:
    msg: "Packages upgraded on {{ inventory_hostname }}: {{ apt_upgrade_result.stdout_lines | select('match', '^Inst ') | map('regex_replace', '^Inst (\\S+).*', '\\1') | list | join(', ') }}"
  when: apt_upgrade_result.changed

- name: Remove packages that are no longer required
  ansible.builtin.apt:
    autoremove: true
    purge: true

- name: Clean apt cache
  ansible.builtin.apt:
    clean: true

- name: Check if a reboot is required
  ansible.builtin.stat:
    path: /var/run/reboot-required
  register: reboot_required_file

- name: Display reboot status
  ansible.builtin.debug:
    msg: "{{ inventory_hostname }} {{ 'requires a reboot' if reboot_required_file.stat.exists else 'does not require a reboot' }}"

- name: Reboot if required
  ansible.builtin.reboot:
    msg: "{{ reboot_message }}"
    reboot_timeout: "{{ reboot_timeout }}"
    pre_reboot_delay: "{{ reboot_pre_delay }}"
    post_reboot_delay: "{{ reboot_post_delay }}"
    test_command: uptime
  become: true
  when:
    - reboot_required_file.stat.exists
    - allow_reboot | bool
